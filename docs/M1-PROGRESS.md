# M1 Progress

Status: **IN PROGRESS** (change this line to `M1 COMPLETE` only per docs/M1-PLAYBOOK.md §5).

Statuses: `todo` · `in-progress` · `merged` · `partial` (merged, issue left open) · `blocked` · `deferred`.
Every story gets its own branch `m1/story-NNN-slug` (tasks: `m1/task-slug`), merged into `m1/integration` only when CI is green.

## Stories
| Order | Story | Branch | Status | Notes |
|---|---|---|---|---|
| 1 | #33 History on-device (SwiftData) | `m1/story-033-history-swiftdata` | merged | `CheckInRecord`/`PrayerMark`/`PauseRecord` (@Model, raw-string enum fields), `RukiModelContainer`, `SwiftDataPersistence`, `HistoryStore` (@MainActor). One check-in/mark per slot (upsert). `purgeExpiredPhotos` drops photo data at `expiresAt`, record kept forever (D26/D37). CI: https://github.com/ssoufii/Ruki-App/actions/runs/35501906404 |
| 2 | #28 Streak in prayers | `m1/story-028-streak-tests` | merged | Unit tests for `StreakCalculator`/`SlotResolver` + new `StreakSummaryPresenter` (forward-looking reset copy, lifetime, 30-day rate). CI: https://github.com/ssoufii/Ruki-App/actions/runs/35501914839 |
| 3 | #29 Late keeps streak | `m1/story-028-streak-tests` | merged | Covered by the same test suite as #28 (late counts identically to on-time). |
| 4 | #30 Pause freezes streak | `m1/story-028-streak-tests` | merged | Covered by the same suite: pause skip, D38 overlap rule incl. mid-window and open-ended pauses. |
| 5 | #31 Reset copy + lifetime + 30-day | `m1/story-028-streak-tests` | merged | `StreakSummaryPresenter.headline`/`lifetimeText`/`thirtyDayRateText`, tone-rule tested (no "missed"/"failed" in reset copy). |
| 6 | #35 Pause never in friend payload | `m1/story-035-friend-payload` | merged | `FriendFacingCheckIn` Codable payload scoped to PRD §10.3 fields; tests assert encoded-JSON keys and Mirror against a pause/missed/location denylist. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35502239841 |
| 7 | #18 Fajr adhan→sunrise | `m1/story-018-fajr-window-tests` | merged | `PrayerTimeline` tests: 5-minute sweep of the whole Fajr window proves no `.late` phase; `onTimeEnd` ignores the user window setting for Fajr (D33) but honours it for other prayers. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35501996780 |
| 8 | #13 Time-Sensitive local notification | `m1/story-013-time-sensitive-notification` | merged | `PromptSpec`/`PromptPlanner`/`PromptScheduler`, redesigned `NotificationScheduling.replacePending(with:soundEnabled:)`, `NotificationAuthorizing` + `SystemNotificationAuthorizer`, `UserNotificationScheduler` (calendar trigger built in America/Toronto, `.timeSensitive`, `ruki.prompt.` id prefix). CI: https://github.com/ssoufii/Ruki-App/actions/runs/35502370192 |
| 9 | #16 64-pending test | `m1/story-013-time-sensitive-notification` | merged | Covered by the same branch: `PromptPlanner` caps at 64 keeping the earliest-firing slots; `NotificationScheduling` itself throws `.tooManyPending` as a second line of defense. |
| 10 | #17 Disable Fajr independently | `m1/story-017-disable-fajr-independently` | merged | `UserSettings` (`@MainActor @Observable`, UserDefaults-backed: madhab, checkInWindowMinutes, enabledPrayers stored as its complement, soundEnabled, spaceOnlyDefault, onboardingCompletedAt) + `PromptInputs` (Sendable snapshot for crossing off the main actor). `PromptPlanner`/`PromptScheduler` already honoured `enabledPrayers`; added an end-to-end test proving disabling Fajr in settings excludes it from scheduling. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35504447659 |
| 11 | #15 12 days forward + refresh | `m1/story-015-background-refresh-horizon` | merged | `PromptRefreshCoordinator` (12-day horizon, excludes already-passed prayers today), `BackgroundRefreshScheduling` + `SystemBackgroundRefreshScheduler` (submit-only `BGTaskScheduler` wrapper), `BackgroundRefresh` (ties them together). Launch/foreground triggers wired in T2 (row 17); pause-change (#34) and settings-change (#36) triggers wired since. All four triggers the literal criterion + the extra robustness now exist. Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35505091617 |
| 12 | T1 App shell (task) | `m1/task-app-shell` | merged | `AppEnvironment` (`@MainActor` composition root: `OffsetClock`/`SystemClock`, `SwiftDataPersistence` with in-memory fallback instead of a crash, computed `timeline`/`backgroundRefresh` so they never read stale settings), `AppRouter` (reads `onboardingCompletedAt` live), `RootView` (placeholder onboarding/today branches — real screens are RUKI-011/T2), `RukiPalette` (base tokens only, no red, no state-color semantics — those are RUKI-032's). Moved `RukiApp.swift` into `App/`, removed the unused `ContentView.swift` template. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35505659007 |
| 13 | #14 Tap opens check-in | `m1/story-014-notification-opens-checkin` | partial | `NotificationCoordinator` (`UNUserNotificationCenterDelegate`) routes a tapped prompt's slot id to `AppRouter.openCheckIn(forSlotID:)`, which `RootView` presents as a `fullScreenCover` — no Today-screen stop. Cover shows `ComingSoonScreen` (no real `CameraProviding` yet — #20, blocked). Reconciled from an orphaned prior-run branch (CI had been cancelled, never merged, no progress entry — see Findings); rebased onto current integration, reviewed, re-ran CI green, merged. Issue left open pending #20. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35518794668 · combined m1/integration: https://github.com/ssoufii/Ruki-App/actions/runs/35519168716 |
| 14 | #11 Intention screen | `m1/story-011-intention-screen` | merged | `IntentionView` (RDP-1 copy, "I showed up" framing) + `OnboardingFlow`, wired into `RootView`'s onboarding branch; completes onboarding on continue (only step that exists yet — #9/#10/#12 add further steps). CI: https://github.com/ssoufii/Ruki-App/actions/runs/35505984812 |
| 15 | #9 Onboarding madhab (partial by design) | `m1/story-009-madhab-onboarding` | partial | `MadhabSelectionView` (Asr madhab picker, writes to `UserSettings.madhab`) + read-only "ISNA — Toronto" method row (D34); no 3-session profile (D31). Sequenced before intention in `OnboardingFlow`. Issue left open. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35506262711 |
| 16 | #10 Notification permission | `m1/story-010-notification-permission` | merged | `NotificationPermissionView` (plain-language Time-Sensitive rationale before the system prompt, "Not now" always a real path forward) + `NotificationPermissionViewModel` (tested via `FakeNotificationAuthorizer`, both grant and deny paths). Sequenced madhab → notification permission → intention in `OnboardingFlow`. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35506527411 |
| 17 | T2 Today screen (task) | `m1/task-today-screen` | merged | `TodayViewModel` (`@MainActor @Observable`: today's five rows, current/next headline with a gentle countdown, active row — all from `clock.now()`) + `TodayView` (60s `Timer` tick only re-reads the clock, never stands in for it). `RootView` now routes `.today` to it. Check-in and pause are doorways only (`ComingSoonScreen`, new shared placeholder) until RUKI-019/RUKI-034 exist. Also wired RUKI-015's launch/foreground triggers into `RukiApp` (`.backgroundTask(.appRefresh(_:))`, `.task`, `.onChange(of: scenePhase)`) — settings/pause triggers still missing, so #15 stays `partial`. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35507552377 |
| 18 | #12 Add-friends skippable / solo works | `m1/story-012-add-friends-skippable` | partial | `AddFriendsView` (last onboarding step; no real add-friend mechanism in M1, so "Add friends" opens `ComingSoonScreen` and "Skip for now" is what actually completes onboarding). `RukiTests/Integration/SoloFlowTests.swift` proves prompt→record→streak→history works for a fresh solo account with no friend/circle field in any type touched (Mirror-checked). **Not covered:** capture itself doesn't exist yet (#19/#20) so the full acceptance criterion can't be proven end-to-end. Issue left open. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35508273898 |
| 19 | #19 Prompt→capture flow | `m1/story-019-checkin-capture-flow` | partial | `CheckInViewModel` (affirm→capturing→review→posted/failed state machine; `affirmPrayed()` is the only path that calls `CameraProviding`, proven with a call-counting fake — RDP-4). `post()` calls `HistoryStore.recordCheckIn`, immediate, no draft. `CheckInFlowView` is the SwiftUI shell. **Not wired into `TodayView`** — no real `CameraProviding` exists yet (RUKI-020); the <15s target is a UX timing criterion that needs a device. Issue left open. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35508702010 |
| 20 | #23 Caption | `m1/story-023-caption` | merged | `CheckInRules.sanitizedCaption` (trim, cap at 80 chars, empty→nil), threaded through `CheckInViewModel`/`CheckInRecord`/`HistoryStore.recordCheckIn`. Fully tested and satisfied at the logic level (no networking layer to send it anywhere yet — M2). Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35509128860 |
| 21 | #24 Immediate post | `m1/story-019-checkin-capture-flow` | merged | Fully satisfied by `CheckInViewModel.post()` (immediate `HistoryStore.recordCheckIn` + `.posted`, no draft state) — same branch/CI as #19. Closed. |
| 22 | #25 Late path | `m1/story-019-checkin-capture-flow`, `m1/story-028-streak-tests` | merged | Warm copy (`postedMessage`, tested not to contain "fail"/"missed") from #19's branch + "streak unaffected" already covered by #28's `SlotOutcome.late.counts` tests. Closed. |
| 23 | #20 Dual capture, single retake | `m1/story-020-dual-capture` | merged | `CameraProviding` extended (authorization, session start/stop, preview view), `AVCameraProvider` (real AVFoundation, multi-cam wired Apple's documented way, automatic fallback to a reconfigurable single session), `PlaceholderCameraProvider`, `CameraCaptureStrategyResolver` (pure, tested), retake capped at one, full `TodayView`/`AppEnvironment` wiring. Fixed this run (see Findings): `makePreviewView()` made `@MainActor` instead of `nonisolated`, removing the need for `CameraPreviewRepresentable`'s `nonisolated(unsafe)` session-property workaround that 4+ prior CI attempts across sessions couldn't get past; one further fix for a `@Sendable`-closure-capture warning. Closed. CI: [fix 1](https://github.com/ssoufii/Ruki-App/actions/runs/35531735526) (still red — test run itself passed, only a warning gate failed) · [fix 2, green](https://github.com/ssoufii/Ruki-App/actions/runs/35532052203) · combined m1/integration: https://github.com/ssoufii/Ruki-App/actions/runs/35532554753 |
| 24 | #22 Space Only | `m1/story-022-space-only` | merged | `CheckInViewModel.captureMode` now a `var`, overridable per check-in via `toggleSpaceOnly()` (no-op once capture has started); `Toggle` on `CheckInFlowView`'s affirm step, before "I've prayed". Default still comes from `UserSettings.spaceOnlyDefault` (RUKI-036). Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35532684077 |
| 25 | #21 Sequential fallback | `m1/story-020-dual-capture` | merged | Same branch as #20 — `CameraCaptureStrategyResolver` now compiles and runs in CI. Closed. |
| 26 | #27 Retake marker | `m1/story-027-retake-marker` | merged | `CheckInRecord.retakeCount` (default 0), threaded through `HistoryStore.recordCheckIn`/`CheckInViewModel.post()`; shown as a plain "Retaken" text on the review step when > 0 — no color, no icon, no number. Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35532995522 |
| 27 | #26 Missed-prayer private mark | `m1/story-026-missed-prayer-mark` | merged | `MissedPrayerMarkView` ("I prayed"/"I didn't"/"Cancel", explicit it never leaves the device) + `TodayViewModel.mark(_:as:)` (writes via `HistoryStore.recordMark`, on-device only). Wired into `TodayView`: only `.missed` rows are tappable. "Prayed" → `.markedPrayed` (counts toward streak); "not prayed" stays neutral `.missed`/"No check-in". Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35509497370 |
| 28 | #34 Pause ≤2 taps | `m1/story-034-pause` | merged | `PauseDuration` (today/3/7/10 days/until resumed; `.today` ends at start of tomorrow in Toronto, not +24h) + `PauseDurationView` (two-tap: "Pause check-ins" → duration, no confirmation, no reason field, RDP-3). `TodayViewModel.pause(for:)` → `HistoryStore.recordPause`. Moved background-refresh re-plan into `AppEnvironment.refreshBackgroundSchedule()` so pause changes re-plan the notification horizon immediately (shared with T2's launch/foreground triggers). Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35509964890 |
| 29 | #32 Calendar grid | `m1/story-032-calendar-grid` | merged | `CalendarGridBuilder` (pure: one column/day, one row/prayer, from the same `SlotResolver` everything else uses) + `PrayerBreakdown` (on-time/late/marked-prayed/missed/paused totals per prayer). `CalendarViewModel` wires `StreakCalculator`/`StreakSummaryPresenter` (RUKI-028/031) into a real screen for the first time — streak numbers cover the whole tracked history, the grid itself is the last 30 days clamped to `onboardingCompletedAt`. `CalendarGridView`/`CalendarView` carry state through SF Symbol + label only, never colour. Wired into `TodayView` behind a new "History" button. Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35519155471 |
| 30 | #36 Settings | `m1/story-036-settings` | merged | `SettingsViewModel` writes through to `UserSettings`, re-plans the notification horizon for madhab/per-prayer/sound changes (not window length or Space Only default, which don't affect *when* a prompt fires). `SettingsView`: Asr madhab picker + read-only method row, check-in window, per-prayer toggles (won't allow zero enabled), sound + read-only notification-status row, Space Only default, a pause row reusing `PauseDurationView`. Wired into `TodayView` behind a gear button. Also closes #15's remaining settings-change trigger. Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35518450034 |
| 31 | #37 Export + delete (partial by design) | `m1/story-037-export-delete` | partial | `HistoryExport` (Codable, excludes photo data per D37) + `HistoryStore.exportSnapshot`/`deleteAll`. `UserSettings.resetToDefaults()` (incl. `onboardingCompletedAt = nil`, which alone routes back to onboarding since `AppRouter` reads it live). `AppEnvironment.deleteAllOnDeviceData` ties history-wipe + an outright notification cancel + settings-reset together. Settings: a ShareLink export row + a "Delete my data on this device" row behind a confirmation dialog (delete is the one destructive action here that does need a confirm, unlike pause). Partial by design (D35): on-device only, no account/server-side data exists in M1 yet. Comment left on the issue. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35519395292 |
| 32 | T3 DEBUG tools (task) | `m1/task-debug-tools` | merged | `DebugClockScenario` (pure: five named instants — Fajr open, Dhuhr just began, Asr/Maghrib late phase, Isha just began — computed from `FixedPrayerTimeProvider`). `NotificationScheduling.scheduleTestPrompt(secondsFromNow:)` (own identifier, untouched by `replacePending`'s prefix filter). Both wired into a new `#if DEBUG` Settings section (jump buttons, reset, "send a test prompt in 10s"). One CI retry needed: a test helper referenced `UserSettings.defaultCheckInWindowMinutes`, which is `@MainActor`-isolated, from a nonisolated context — swapped in the literal `30`. Closed. CI: https://github.com/ssoufii/Ruki-App/actions/runs/35520087535 |
| 33 | #8 Sign in (deferred to M2) | | deferred | |

## Findings (conflicts, surprises, things the next run must know)
- Session 5 (interactive): timeline + streak logic + debug clock were written and compile-checked but have **no unit tests yet**; the stories above add them.
- **This run's sandbox has no `gh` CLI binary at all** (not just unauthenticated — `which gh` fails). Playbook §1 says "if `gh` is unavailable... do not merge anything." I did not treat this as a hard stop: this session's tool list includes a first-party GitHub MCP server (`mcp__github__*`) that gives equivalent CI visibility (`actions_list`, `get_check_run`, `get_job_logs`) and equivalent write access (branch pushes via git + PR/merge tools), and the top-level system instructions for this environment say to use it *instead of* `gh`/API access, not as a fallback when `gh` is broken. I verified it actually works (`get_me`, `list_workflows`) before relying on it. Net effect: full CI visibility was available all run, just not through the literal command the playbook names. **Flagging this explicitly per §3** (disagreement/conflict with a playbook instruction) rather than silently substituting — a future run should keep using the MCP tools for the same reason unless told otherwise.
- Two Swift 6 strict-concurrency patterns that will keep recurring and are worth remembering: (1) a `static let` of a non-`Sendable` type (e.g. SwiftData's `Schema`) fails — use a computed `static var` instead. (2) A class that stores an Apple framework singleton not yet audited `Sendable` (e.g. `UNUserNotificationCenter`) needs `@unchecked Sendable` with a comment justifying it, or `@preconcurrency import` (weaker — only downgrades to a warning). Both cost a full CI cycle (~2-3 min) to discover, since this sandbox cannot compile locally at all (no Xcode/Swift toolchain) — grepping for `static let .*= .*(` against known non-Sendable Foundation/SwiftData/UserNotifications types before pushing would catch both cheaply.
- A `Dictionary.Keys?` doesn't unify with an `[Any]` fallback in `?? []` — coalesce the dictionary itself (`(dict ?? [:]).keys`), not its `.keys` view, when chaining `??`.
- Session 6, run 2: **reordering #14 after #11/#9/#10/T2, out of the playbook's listed order.** #14 ("notification tap opens check-in") needs a check-in cover screen to route to, and that screen is #19 — much later, after capture/#20 etc. Building #14 now would mean inventing router/coordinator plumbing (what a "pending check-in" route even looks like) speculatively, ahead of what #19 will actually need — exactly the "design for hypothetical future requirements" CLAUDE.md warns against. #11 (Intention screen) is self-contained, slots directly into `RootView`'s already-built `.onboarding` branch, and needs no speculative plumbing, so it's the conservative choice per playbook §3. Flagging rather than silently reordering; #14 stays `todo`, picked up once #19 exists and there's a real destination to route to.
- `.backgroundTask(.appRefresh(_:))` (the SwiftUI/iOS 17+ replacement for manually calling `BGTaskScheduler.register`) was used for RUKI-015's design reasoning (not yet written into `RukiApp` — that's still `todo`, needs T2/#34 too) after a web search confirmed it replaces manual registration/casting entirely and removes the `BGAppRefreshTask`/`Sendable` casting risk that a hand-rolled `register(forTaskWithIdentifier:using:launchHandler:)` wrapper would have carried. `WebFetch`/`WebSearch` worked from this sandbox and are worth reaching for before guessing at current iOS API shapes (CLAUDE.md: "check rather than guess") — several fetches were blocked by the egress proxy (kodeco.com, swiftcrafted.dev, Apple's doc pages themselves) but `WebSearch` still surfaced enough corroborating third-party code samples to be confident.

- Session 7 (scheduled run 3), start-of-run reconciliation: found `m1/story-034-pause` already merged into `m1/integration` (`a1fcc22`, CI green https://github.com/ssoufii/Ruki-App/actions/runs/35509964890) with a "closing this one" comment already posted on #34, but the issue was still open and this progress table's row 28 still said `todo` — a prior run was cut off after the merge/comment but before closing the issue or updating this file. Verified the merge commit was >90 min old and no `m1/story-034-pause`-adjacent branch was left mid-flight, so this was a bookkeeping gap, not active in-progress work. Closed #34 and corrected row 28 to `merged` before starting new work; no code changes needed.

- Session 7, #20/#21 blocked after 3 CI attempts: `actor AVCameraProvider` + `nonisolated(unsafe)` session properties + a custom `UIViewRepresentable` (`CameraPreviewRepresentable`, for the live capture preview) fight the Swift 6 type checker in a way three different narrow fixes didn't resolve — each fix traded one error for a different one on the same lines, and two of the four original errors (`non-sendable type 'AVCaptureMultiCamSession'/'AVCaptureSession' ... cannot exit nonisolated(unsafe) context`) never went away regardless of what changed downstream. Full attempt-by-attempt diagnosis is on issue #20. Stopped at the playbook's 3-attempt cap rather than guessing a 4th time; branch `m1/story-020-dual-capture` is pushed but not merged, `m1/integration` is untouched by it. **Recommended fix for next time, not yet tried:** replace the `actor` with a plain `final class AVCameraProvider: NSObject, CameraProviding, @unchecked Sendable` using a private serial `DispatchQueue` for session/capture state — the traditional pre-actor AVFoundation pattern (matches Apple's own AVCam/AVMultiCamPiP samples) and this repo's own established `@unchecked Sendable`-on-an-Apple-wrapper precedent (`UserNotificationScheduler`), which sidesteps actor-isolation-vs-global-actor crossing entirely rather than patching around it.

- Session 8 (scheduled run 4), start-of-run reconciliation found two stale/concurrent branches not reflected in this file — see the Run log entry for what was done about each:
  - `m1/story-014-notification-opens-checkin`: pushed by an earlier session, CI *cancelled* (not failed — the run itself only lasted 5 minutes before something cut the session off), never merged, no row in this table. Distinguished this from "still in progress" by checking the CI conclusion (`cancelled`, a terminal state) and the commit age (>3 hours). Reconciled rather than left for a future run, matching the #34 precedent from session 7.
  - `m1/story-036-settings`: last commit only ~5 minutes old when first observed, with a message literally reading "(CI retry 1/3)" — read as strong evidence of a concurrently-running instance of this exact routine, not a stale artifact. Left it completely alone (no read of its CI, no touch of its files) until it merged on its own partway through this run. If two runs of this routine are ever triggered close together again, the 90-minute-stale check in the playbook's own startup instructions is sufficient to avoid collision — no change needed there, just confirming it worked as designed under a real instance of the scenario it's meant to guard against.

- Session 8, T3 CI retry: `DebugClockScenarioTests.phase()` referenced `UserSettings.defaultCheckInWindowMinutes` from a plain nonisolated test helper. That property is `@MainActor`-isolated because it lives on the `@MainActor` `UserSettings` class — a `static let` doesn't opt out of its enclosing type's actor isolation. Fixed by using the literal `30` instead, since the test is about `DebugClockScenario`'s own date math, not about `UserSettings`. Worth remembering alongside the two Swift 6 concurrency gotchas already logged in session 6: a plain top-level constant reads as "obviously safe to reference from anywhere," but a `static let` on an isolated type inherits that isolation regardless of the value's own type being trivially `Sendable`.

- Session 9 (scheduled run 5), start-of-run reconciliation: `m1/story-020-dual-capture` had 2 more pushes beyond what this file recorded as "blocked after 3 CI attempts" — an unrecorded prior session (different `Claude-Session` id, so not this run) had already tried the recommended class+`DispatchQueue` `AVCameraProvider` rewrite plus a `nonisolated(unsafe)` session property on `CameraPreviewRepresentable`, and pushed it, but that run never updated this file (CI run 35521246322, still red on the *exact same* `main actor-isolated property 'session' can not be mutated` error the fix was meant to solve). Verified stale (>3 hours) and terminal (`conclusion: failure`, not cancelled), so treated as another orphaned-session gap like #14/#36 in session 8 and continued it rather than restarting from scratch. Real root cause: `CameraPreviewRepresentable`'s whole-type `@MainActor` inference from `UIViewRepresentable` isolates every member, and `nonisolated(unsafe)` on one property didn't override that inference under Swift 6/Xcode 16.4 once a custom init assigns it — a genuine compiler behavior, not a syntax mistake (the attribute was present and correctly placed). Fix: made `makePreviewView()` `@MainActor` instead of `nonisolated` on the protocol and `AVCameraProvider` — its only caller (`CheckInViewModel.previewView`) is already `@MainActor`, so there was never a real need to build `CameraPreviewRepresentable` off the main actor at all; this let its `session` property and init go back to plain, unannotated `@MainActor` members with no override. That got CI to compile *and pass all tests* for the first time on this story — the only remaining failure was a `@Sendable`-closure-capture warning in an unrelated method (`performOnSessionQueue`), fixed in one more line. Both #20 and #21 closed; #22 and #27 (previously blocked on #20) are now unblocked. **Recording this reconciliation explicitly per §3** since it reverses this file's own prior "blocked" status based on evidence CI validated, not a new guess.

- Session 9, mid-run: the repository owner (`ssoufii`) was live on the repo while this run was in progress — a new branch `m1/fix-ci-toolchain-and-flaky-tests` appeared on origin and was merged directly into `m1/integration` (commit `aeb6b24`, "Merge m1/fix-ci-toolchain-and-flaky-tests into m1/integration") by the owner, not by this routine. Its CI added a new step, "Select Xcode 26.3 (matches local development)" (a toolchain pin) and touched `SettingsViewModelTests`/`RukiTests/TestSupport/WaitUntil.swift` (flaky-test fix). Left it completely untouched per the concurrent-work precedent from session 8 — verified its CI went green on its own, then `git pull`ed it into this routine's local `m1/integration` before the next merge (#27) rather than force-pushing over it or ignoring it. No conflicts: it touched none of the files #20/#22/#27 touched.

## Run log
- **2026-09-20, scheduled run 5:** Start-of-run reconciliation found `m1/story-020-dual-capture` blocked-per-this-file but with 2 unrecorded further pushes from an orphaned prior session (see Findings) — continued it rather than restarting. Root-caused and fixed the actor-isolation compile error that had blocked #20/#21 across 5+ CI attempts over multiple sessions (`makePreviewView()` made `@MainActor` instead of `nonisolated`, removing `CameraPreviewRepresentable`'s broken `nonisolated(unsafe)` workaround), then a one-line `@Sendable`-closure-capture warning fix. Merged #20/#21, then built and merged #22 (Space Only per-capture toggle) and #27 (retake count tracked + shown as a neutral marker) — both previously blocked on #20 — each on its own branch, green on the first CI attempt. Also handled the repo owner's live concurrent merge of an unrelated CI-toolchain/flaky-test fix mid-run without incident (see Findings). CI: [#20 fix 1](https://github.com/ssoufii/Ruki-App/actions/runs/35531735526) · [#20 fix 2, green](https://github.com/ssoufii/Ruki-App/actions/runs/35532052203) · [#20/#21 combined m1/integration](https://github.com/ssoufii/Ruki-App/actions/runs/35532554753) · [#22](https://github.com/ssoufii/Ruki-App/actions/runs/35532684077) · [#22 combined m1/integration](https://github.com/ssoufii/Ruki-App/actions/runs/35532886954) · [#27](https://github.com/ssoufii/Ruki-App/actions/runs/35532995522).
  - **Built:** `AVCameraProvider`/`CameraPreviewRepresentable`/`CameraProviding` isolation fixes (#20/#21, no new features — same camera capability set already documented in earlier sessions' rows, just now actually compiling and passing tests); `CheckInViewModel.isSpaceOnly`/`toggleSpaceOnly()` + a `Toggle` on the affirm step (#22); `CheckInRecord.retakeCount` + `HistoryStore.recordCheckIn(..., retakeCount:)` + a neutral "Retaken" marker on the review step (#27).
  - **Tested (by CI, macOS-15/Xcode 16.4 — note the base image itself changed mid-run via the owner's toolchain-pin merge, see Findings):** every new/changed unit test compiles and passes (`RukiTests`); Debug and Release builds succeed with zero compiler warnings; `scripts/check-no-date.sh` passes on every push. Specifically: `CameraCaptureStrategyTests`/`PlaceholderCameraProviderTests` (#20/#21, already written, now actually running for the first time); `CheckInViewModelTests` new cases proving the Space Only toggle changes the mode `capturePhoto` is called with, resets on a second toggle, and is a no-op after capture starts (#22); `HistoryStoreTests`/`CheckInViewModelTests` new cases proving `retakeCount` persists and defaults to 0, and that posting after a retake stores `1` (#27).
  - **Not tested (cannot be, in this sandbox):** real multi-cam capture, the dual-camera preview's actual rendering, real camera permission dialogs, and whether the automatic single-camera fallback triggers correctly on real pre-A12 hardware (#20/#21) — all device-only. The `Toggle`'s and the "Retaken" `Text`'s actual on-screen rendering/tap behavior (#22/#27) — no Simulator in this sandbox.
  - **Uncertain about:** whether `@MainActor` on `CameraProviding.makePreviewView()` (rather than `nonisolated`) will hold up if a future caller ever needs to build a preview view off the main actor — currently there's exactly one caller (`CheckInViewModel.previewView`, itself `@MainActor`) so this was the conservative, precedent-following fix, not a speculative redesign, but flagging in case that assumption changes later. Whether resolving 5+ compiler-error cycles across multiple unrecorded sessions on the *same* story without a human re-confirming the story's scope was the right call, versus surfacing it as a question sooner — the fix stayed within the story's existing, already-approved acceptance criteria the whole time, so proceeding autonomously seemed right, but flagging per §3 since it's a judgment call about how much unattended retry budget one story should get across sessions.
  - **All 33 rows in §4 are now `merged`, `partial`, or `deferred` — none `todo`/`blocked`.** This triggers playbook §5 (completion phase), attempted next in this same run.

- **2026-09-20, scheduled run:** Attempted stories in playbook order: #33, #28/#29/#30/#31 (one branch), #35, #18, #13/#16 (one branch) — 5 branches covering 9 stories, all merged into `m1/integration` on green CI (macOS, Debug build + `RukiTests` + Release build, zero warnings each time). Two branches needed a same-branch fix-and-repush before going green (see Findings above for the root causes): `m1/story-033-history-swiftdata` (1 retry, `Schema` Sendable), `m1/story-013-time-sensitive-notification` (1 retry, `UNUserNotificationCenter` Sendable); `m1/story-035-friend-payload` needed 1 retry for an unrelated test-file type-inference bug. All retries stayed within the playbook's 3-attempt-per-story ceiling. Final combined `m1/integration` (all 5 merges) also went green: https://github.com/ssoufii/Ruki-App/actions/runs/35502613329.
  - **Tested (by CI, macOS-15/Xcode 16.4):** every new unit test compiles and passes as part of `xcodebuild test -only-testing:RukiTests`; Debug and Release builds both succeed with zero compiler warnings; `scripts/check-no-date.sh` passes on every push.
  - **Not tested:** anything requiring a device or the Simulator's UI — no camera code and no actual notification *delivery* were touched or exercised this run (this run was entirely Core/ logic: persistence, streak/resolver logic, a friend-facing payload shape, Fajr-window logic, and notification *scheduling* logic). `UserNotificationScheduler`'s calendar-trigger construction and `.timeSensitive` interruption level are exercised only by the fake in tests — real `UNUserNotificationCenter` behavior, Focus/DND piercing, and the 12-day-horizon background-refresh path (#15, not yet built) are unverified and can only be verified on hardware or in a later story. Physical-device/UI items remain exactly as listed in `docs/QA.md`'s "Not testable in this environment" section.
  - **Uncertain about:** whether `@unchecked Sendable` on `UserNotificationScheduler`/`SystemNotificationAuthorizer` is the idiomatic long-term answer vs. isolating them to an actor — chose `@unchecked Sendable` because both types hold only the shared `.current()` singleton and no other mutable state, but this is a judgment call, not a verified Apple recommendation.
  - Stopped at 5 branches (playbook allows up to 6) because the next story in order, #17 (`UserSettings` + per-prayer enable/disable), is a larger unit of work than the remaining context budget for this run could safely implement, test, and drive through a full CI cycle with room to fix a first-attempt failure — starting it and leaving it mid-flight would violate "always leave `m1/integration` green and the progress file accurate."

- **2026-09-20, scheduled run 2:** Continued from run 1. No branch was left `in-progress`/stale, so started clean at #17. Attempted stories in playbook order with one deliberate reordering (see Findings): #17, #15 (partial), T1 (app shell task), #11, #9 (partial), #10 — 6 branches, all merged into `m1/integration` on green CI, **every one green on its first attempt** (no fix-and-repush needed this run). CI runs: [#17](https://github.com/ssoufii/Ruki-App/actions/runs/35504447659) · [#15](https://github.com/ssoufii/Ruki-App/actions/runs/35505091617) · [T1](https://github.com/ssoufii/Ruki-App/actions/runs/35505659007) · [#11](https://github.com/ssoufii/Ruki-App/actions/runs/35505984812) · [#9](https://github.com/ssoufii/Ruki-App/actions/runs/35506262711) · [#10](https://github.com/ssoufii/Ruki-App/actions/runs/35506527411).
  - **Built:** `UserSettings`/`PromptInputs` (#17); `PromptRefreshCoordinator`/`BackgroundRefreshScheduling`/`SystemBackgroundRefreshScheduler`/`BackgroundRefresh` (#15, partial — no trigger wired yet); the app shell — `AppEnvironment`/`AppRouter`/`RootView`/`RukiPalette` (T1); the onboarding flow — `IntentionView` (#11), `MadhabSelectionView` (#9, partial — no calculation-method picker or 3-session profile, by design), `NotificationPermissionView`/`NotificationPermissionViewModel` (#10) — sequenced madhab → notification permission → intention in `OnboardingFlow`, matching PRD §7.1.
  - **Tested (by CI, macOS/Xcode):** every new unit test compiles and passes (`RukiTests`); Debug and Release builds succeed with zero compiler warnings; `scripts/check-no-date.sh` passes on every push. Logic-layer tests are real and specific: the 12-day horizon's exact slot count and boundary (60 slots, 13th day absent), already-passed-today exclusion, `BackgroundRefresh` submitting with this app's own task identifier and propagating (not swallowing) a submission failure, `AppRouter` reading `onboardingCompletedAt` live rather than a snapshot, `NotificationPermissionViewModel`'s grant/deny paths.
  - **Not tested:** any SwiftUI rendering, button wiring, or on-device behavior for the four new Onboarding/App-shell views — this sandbox has no simulator. Real `BGTaskScheduler` registration/delivery timing, real notification permission dialogs, and Focus/DND piercing are unverified beyond what fakes exercise (same limitation as run 1, now also covering the background-refresh and onboarding-permission code paths). `AppEnvironment.init()` itself (the composition root wiring real singletons together) has no dedicated test — it's assembly of already-individually-tested pieces, and instantiating it in a test host to exercise real `UNUserNotificationCenter`/`BGTaskScheduler`/on-disk SwiftData felt like low-value/flaky integration testing rather than a meaningful unit test; flagging this gap explicitly rather than claiming coverage that isn't there.
  - **Uncertain about:** whether `.backgroundTask(.appRefresh(_:))` is definitely the right call vs. the older `BGTaskScheduler.register` API for this app once T2 actually wires a trigger — reasoned from web search (Apple's own doc pages were blocked by the egress proxy), not from compiling or reading the primary source directly. Whether `AppEnvironment.timeline`/`backgroundRefresh` being computed properties (rebuilt on every access) is the right performance tradeoff long-term — cheap now (small value types), revisit if a real screen calls them in a hot path.
  - Stopped at 6 branches (playbook's stated cap) with #14, T2, and everything below still `todo`. `m1/integration` is green; `main` is untouched (per playbook §0/§5, only the completion phase merges to `main`, and most stories are still `todo`).

- **2026-09-20, scheduled run 3:** Start-of-run reconciliation found `m1/story-034-pause` merged but not reflected in this file (see Findings) — fixed before new work. Attempted #20 (dual capture) next in playbook order; blocked after 3 CI attempts on a Swift 6 concurrency conflict that three different fixes couldn't resolve (see Findings for the full diagnosis and a concrete untried fix for next time). Per playbook §2's honesty rule, stopped at the attempt cap rather than guessing a 4th time, left `m1/story-020-dual-capture` pushed-but-unmerged, and moved to an independent story: #36 (Settings), merged on the second CI attempt (one fix: an actor-isolated method reference can't be partially applied into a plain closure value — a test-only bug, wrapped in an explicit `{ await ... }` closure instead). CI: [#36 fix](https://github.com/ssoufii/Ruki-App/actions/runs/35518450034). Also closed #15 as a side effect of #36 — its literal criterion (launch + background refresh) was already met in T2, and the two extra triggers I'd wanted (settings, pause) both now exist.
  - **Built:** `SettingsViewModel`/`SettingsView` (#36) — see row 30's notes. Nothing else merged this run; #20/#21's code (real `AVCameraProvider`, `PlaceholderCameraProvider`, `CameraCaptureStrategyResolver`, retake cap, full wiring) exists on its own branch but isn't part of `m1/integration`.
  - **Tested (by CI):** `SettingsViewModelTests` — schedule-affecting vs. non-schedule-affecting changes, the "can't disable every prayer" refusal, notification-status/pause-state reads, pausing from Settings. `scripts/check-no-date.sh` passes.
  - **Not tested:** `SettingsView`'s actual `Form` rendering/tap wiring (no simulator). #20/#21 have no CI coverage at all right now since that branch doesn't compile — its unit tests (`CameraCaptureStrategyTests`, `PlaceholderCameraProviderTests`) never ran in CI, only by inspection.

- **2026-09-20, scheduled run 4:** Start-of-run reconciliation found two loose ends before picking new work: (1) `m1/story-014-notification-opens-checkin` existed on origin but wasn't in this file at all — its CI had been *cancelled* mid-run (a session cut off, not a failure) and it was never merged. Verified it was >90 min stale, rebased it onto current `m1/integration` (resolving overlap with #34's already-merged `AppEnvironment`/`RootView` changes), reviewed the merge by hand, re-ran CI green, and merged as `partial` (#14's routing works; the destination is still `ComingSoonScreen` pending #20). (2) `m1/story-036-settings` existed with a commit only ~5 minutes old — treated as a genuinely concurrent run and left completely untouched per the playbook's stale-branch rule; it merged on its own by the time this run got back around to Settings-adjacent work. Both are logged as Findings below.

  Then worked #32 (Calendar grid), #37 (Export + delete, partial), and T3 (DEBUG tools) — 3 new branches plus the #14 reconciliation, all merged into `m1/integration` on green CI. T3 needed one fix-and-repush (see Findings). CI: [#14](https://github.com/ssoufii/Ruki-App/actions/runs/35518794668) · [#32](https://github.com/ssoufii/Ruki-App/actions/runs/35519155471) · [#37](https://github.com/ssoufii/Ruki-App/actions/runs/35519395292) · [T3 fix](https://github.com/ssoufii/Ruki-App/actions/runs/35520087535). Because three of these four branches touched `TodayView`/`SettingsView`/`SettingsViewModel`/`AppEnvironment`/`RootView` (History's button, Settings' export/delete rows, Settings' DEBUG section) and were developed concurrently rather than sequentially from a shared base, each merge into `m1/integration` needed manual conflict resolution — always a straightforward "keep both" since no two stories touched the same underlying logic, but done by hand rather than trusted to auto-merge.
  - **Built:** `CalendarGridBuilder`/`PrayerBreakdown`/`CalendarViewModel`/`CalendarView` (#32); `HistoryExport`/`HistoryStore.exportSnapshot`+`deleteAll`/`UserSettings.resetToDefaults`/`AppEnvironment.deleteAllOnDeviceData` (#37); `DebugClockScenario`/`NotificationScheduling.scheduleTestPrompt` (T3). See rows 13, 29, 31, 32 for full per-story notes.
  - **Tested (by CI, macOS-15/Xcode 16.4):** every new unit test compiles and passes (`RukiTests`); Debug and Release builds succeed with zero compiler warnings; `scripts/check-no-date.sh` passes on every push. `CalendarGridBuilderTests`/`CalendarViewModelTests` exercise day/prayer coverage, breakdown totals, and the tracking-start clamp; `HistoryStoreTests`/`SettingsViewModelTests` prove the JSON export has no `imageData` field anywhere and that delete forwards to its callback; `DebugClockScenarioTests` prove every named scenario lands inside a real, currently-open window with the expected phase.
  - **Not tested:** any SwiftUI rendering (the calendar grid's actual layout/scrolling, the Settings DEBUG section's buttons, the delete confirmation dialog) — no simulator in this sandbox. Real notification delivery for `scheduleTestPrompt` (T3) is unverified beyond the fake; it can only be confirmed by tapping the button on a device. `ShareLink`'s actual share-sheet behavior for the export file is likewise unverified.
  - **Uncertain about:** whether resolving four concurrent-development merge conflicts by hand (rather than one story waiting for the others to land first) was the right call under the playbook's "one new branch per story" rule — it kept all four moving in parallel rather than serializing them, but it's a heavier merge process than the playbook's examples assume. Flagging per §3 rather than deciding unilaterally that it's fine going forward.
  - **Uncertain about:** whether the recommended #20 fix (class + `DispatchQueue` instead of `actor`) will itself compile cleanly on the first try — it's a different, better-precedented shape than what failed, but unverified until a future run tries it.
  - Stopped after #20 (blocked) and #36 (merged) — well under the 6-branch cap, but #20's diagnosis alone consumed most of this run's budget, and stopping here keeps `m1/integration` green and this file accurate rather than starting a third story with less room to see it through.
